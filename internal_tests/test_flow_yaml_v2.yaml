agents:

    biographer:
        role: Write a biography
        tools: WikipediaTool

    info:
        role: Get some information about all the main topics mentioned except the one mentioned in the query.
        tools: WikipediaTool
        query: false
        context:
            - biographer

    summarizer:
        role: Summarize the text and write at the end 'Written by Gwenlake Agents'.
        query: false
        context:
            - biographer
            - info

    # pptx:
    #     task: Prepare a beautiful and professionnal Powerpoint file (PPTX format) summarizing the information given.
    #     tools: python_repl
    #     context:
    #         - summarizer
